<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.first.materialdesign">

    <uses-feature android:name="android.hardware.camera"
                  android:required="true" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_laucher_git"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_laucher_git_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".Activity.SplashScreenActivity"
                android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".Activity.DokuActivity">
        </activity>
        <activity
                android:name=".Activity.GowesActivity"
                android:label="@string/title_activity_gowes">
        </activity>
        <activity android:name=".Activity.HutangActivity">
        </activity>
    </application>

</manifest>